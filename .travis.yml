sudo: required

language: python

services:
  - docker
  - postgresql

addons:
  postgresql: "9.3"

before_install:
  - docker build -t impactlab/oeem-client .
  - docker ps -a
  - docker run impactlab/oeem-client

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres

script:
  - python manage.py test

